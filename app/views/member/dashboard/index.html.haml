%h1= current_page_title

- if @lists.blank?
  %p You have no secret santa lists yet!
%p= link_to "Create a list", new_member_list_path, class: "btn btn-primary btn-lg"

- if @lists.any?
  %h3 Your Secret Santa lists
  %table.table.table-hover
    %thead
      %tr
        %th List Name
        %th.actions Actions
      %tbody
        - @lists.each do |list|
          %tr
            %td= list.name
            %td.actions
              = link_to "View", member_list_path(list), class: "btn btn-small btn-default"
              - unless list.is_locked || list.gift_day_cannot_be_in_the_past
                = link_to "Edit", edit_member_list_path(list), class: "btn btn-small btn-default"
                = link_to "Delete", member_list_path(list), class: "btn btn-small, btn-danger",
                  data: { confirm: "Are you sure you want to delete this list?" },
                  method: :delete
              - else
                = link_to "Copy list", member_list_copy_list_path(list),
                  class: "btn btn-small btn-default",
                  method: :post
